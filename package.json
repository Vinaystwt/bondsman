{
  "name": "bondsman",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build:contracts": "cd contracts && cargo odra build",
    "deploy": "npm --workspace backend run deploy",
    "redeploy": "npm --workspace backend run redeploy",
    "agent": "npm --workspace backend run agent",
    "integrator": "npm --workspace backend run integrator",
    "listener": "npm --workspace backend run listener",
    "watchdog": "npm --workspace backend run watchdog",
    "mcp": "npm --workspace backend run mcp",
    "api": "npm --workspace backend run api",
    "seed": "npm --workspace backend run seed",
    "demo:prearm": "npm --workspace backend run demo:prearm",
    "test": "npm --workspace backend test",
    "typecheck": "npm --workspace backend run typecheck",
    "demo": "npm --workspace backend run demo"
  }
}
